meta {
  name: Upload Image
  type: http
  seq: 1
}

post {
  url: {{baseUrl}}/images
  body: multipartForm
  auth: bearer
}

auth:bearer {
  token: {{authToken}}
}

body:multipart-form {
  image: @file(/path/to/image.jpg)
}
