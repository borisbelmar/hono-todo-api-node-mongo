meta {
  name: Update Todo (PATCH)
  type: http
  seq: 5
}

patch {
  url: {{baseUrl}}/todos/:id
  body: json
  auth: bearer
}

params:path {
  id: 
}

auth:bearer {
  token: {{authToken}}
}

body:json {
  {
    "completed": true
  }
}
